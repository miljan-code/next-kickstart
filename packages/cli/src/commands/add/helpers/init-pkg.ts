import { type Packages } from "@/commands/init/helpers/prompts.js";
import { type AvailablePackage } from "./check-argument.js";

export const initPkg = (packages: Packages, pkg: AvailablePackage) => {};
